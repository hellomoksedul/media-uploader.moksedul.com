// Not used. This package targets Tailwind CSS v4, which is configured
// CSS-first in src/index.css (@theme / @source). This file is intentionally
// a no-op and is NOT loaded by @tailwindcss/postcss (v4 requires an explicit
// `@config` directive to load a JS config). Kept only to avoid stale imports.
module.exports = {};
